#include <algorithm>
#include <cstring>
#include <iostream>

#define INF 10000
int N = 4;
int w[INF] = {2, 1, 3, 2};
int v[INF] = {3, 2, 4, 2};
int W = 5;
int dp[INF];

auto main() -> decltype(0) {
  memset(dp, -1, sizeof(dp));
  return 0;
}
